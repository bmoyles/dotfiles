if [[ -z ${POWERLINE_TMUX_BINDINGS:-} ]] && which powerline &>/dev/null; then
    export POWERLINE_TMUX_BINDINGS="$(python -c 'import os, powerline; print(os.path.dirname(powerline.__file__))')/bindings/tmux/powerline.conf"
fi
=tmux ${TMUXOPTS} "$@"

# vim:filetype=zsh ts=4 sw=4 sts=4 et
